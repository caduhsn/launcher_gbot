/**
• JSON configuration file for the Client Updater
• clientVersion Versão do client (dispara update quando muda)
• launcherVersion Versão do launcher (apenas exibida na UI no código atual)
• clientFolder Pasta raiz do client dentro do diretório do launcher (ex.: "Tibia")
• newClientUrl URL do ZIP completo (primeira instalação / fallback)
• clientExecutable Nome do executável em "<clientFolder>/bin/" (ex.: "client.exe")
• replaceFolders / replaceFolderName Usado apenas no fluxo ZIP (apaga pastas antes de extrair)

• updateFiles (incremental)
  - Se estiver preenchido E o client já estiver instalado, o launcher baixa somente os arquivos listados
  - path: caminho relativo dentro de <clientFolder> (use "/" como separador)
  - url: link direto do arquivo no seu servidor/CDN/GitHub
  - sha256: opcional; se vazio/ausente, não valida integridade
*/
{
	/* Troque para a nova versão sempre que publicar um update */
	"clientVersion": "1.0",
	
	/* Opcional: versão do launcher (UI) */
	"launcherVersion": "1.0",
	
	/* Se true, apaga pastas listadas antes de extrair o ZIP completo */
	"replaceFolders": false,
	"replaceFolderName": [
		{
			"name": "data/things/1511"
		}
	],
	
	/* Pasta do client dentro do diretório do launcher */
	"clientFolder": "Client_Online-GBOT",
	
	/* ZIP completo: usado na primeira instalação (quando não existe bin/client.exe) */
	"newClientUrl": "https://github.com/caduhsn/launcher_gbot/releases/download/1.0/otcliente-gbot.zip",
	
	/* Executável do client dentro de <clientFolder>/bin/ */
	"clientExecutable": "otclient_dx_x64.exe",
	
	/* Updates incrementais (somente arquivos alterados) */
	"updateFiles": []
}
